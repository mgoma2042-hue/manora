<!DOCTYPE html>
<html lang="ar">
<head>
<meta charset="UTF-8">
<title>عيد ميلاد سعيد ❤️</title>

<style>
body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: linear-gradient(120deg, #ff416c, #ff9a9e);
    color: white;
    text-align: center;
    overflow-x: hidden;
}

header {
    padding: 60px 20px;
}

h1 {
    font-size: 42px;
}

.timer {
    font-size: 22px;
    background: rgba(255,255,255,0.2);
    padding: 15px 25px;
    border-radius: 25px;
    display: inline-block;
    margin-top: 15px;
}

.gallery {
    display: flex;
    justify-content: center;
    gap: 15px;
    flex-wrap: wrap;
    margin: 40px 20px;
}

.gallery img {
    width: 240px;
    height: 300px;
    object-fit: cover;
    border-radius: 20px;
    box-shadow: 0 10px 20px rgba(0,0,0,0.3);
}

.card {
    background: rgba(255,255,255,0.2);
    margin: 15px auto;
    padding: 20px;
    border-radius: 20px;
    max-width: 500px;
    font-size: 18px;
}

button {
    background: white;
    color: #ff416c;
    border: none;
    padding: 15px 30px;
    border-radius: 30px;
    font-size: 18px;
    cursor: pointer;
    margin: 30px;
}

button:hover {
    transform: scale(1.1);
}

#surprise {
    display: none;
    font-size: 22px;
    margin-bottom: 40px;
}

footer {
    padding: 30px;
    font-size: 18px;
}

/* قلوب متحركة */
.heart {
    position: fixed;
    color: pink;
    font-size: 20px;
    animation: float 5s linear infinite;
}

@keyframes float {
    from {
        transform: translateY(100vh);
        opacity: 1;
    }
    to {
        transform: translateY(-10vh);
        opacity: 0;
    }
}
</style>
</head>

<body>

<!-- 🎵 موسيقى -->
<audio autoplay loop>
    <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<header>
    <h1>🎂 عيد ميلاد سعيد يا أغلى الناس ❤️</h1>
    <p>عدّى على حبنا</p>
    <div class="timer" id="timer"></div>
</header>

<!-- الصور -->
<section class="gallery">
    <img src="https://images.unsplash.com/photo-1529626455594-4ff0802cfb7e">
    <img src="https://images.unsplash.com/photo-1517841905240-472988babdf9">
    <img src="https://images.unsplash.com/photo-1500648767791-00dcc994a43e">
</section>

<!-- رسائل -->
<div class="card">❤️ من أول يوم عرفتك وأنا حاسس إن ربنا بيكرمني</div>
<div class="card">🌹 كل سنة وإنتِ أجمل حاجة حصلت في حياتي</div>
<div class="card">💌 وجودك جنبي هو الأمان</div>

<!-- زر مفاجأة -->
<button onclick="showSurprise()">🎁 اضغطي هنا</button>

<div id="surprise">
    💍 المفاجأة إنك أحلى هدية في حياتي  
    <br> وبحبك أكتر كل يوم ❤️
</div>

<footer>
    معمول مخصوص ليكي ❤️
</footer>

<script>
// ⏳ عداد الوقت (غير التاريخ)
const startDate = new Date(2023, 0, 1);

function updateTimer() {
    const now = new Date();
    const diff = now - startDate;

    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
    const minutes = Math.floor((diff / (1000 * 60)) % 60);
    const seconds = Math.floor((diff / 1000) % 60);

    document.getElementById("timer").innerHTML =
        `${days} يوم | ${hours} ساعة | ${minutes} دقيقة | ${seconds} ثانية`;
}

setInterval(updateTimer, 1000);
updateTimer();

// 🎁 مفاجأة
function showSurprise() {
    document.getElementById("surprise").style.display = "block";
}

// 💖 قلوب متحركة
setInterval(() => {
    const heart = document.createElement("div");
    heart.className = "heart";
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.fontSize = Math.random() * 20 + 15 + "px";
    document.body.appendChild(heart);

    setTimeout(() => {
        heart.remove();
    }, 5000);
}, 300);
</script>

</body>
</html>

